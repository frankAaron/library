<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.dao.CategoryMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_category (category_name, category_code, description)
        VALUES (#{categoryName}, #{categoryCode}, #{description})
    </insert>

    <update id="update">
        UPDATE t_category
        <set>
            <if test="categoryName != null and categoryName != ''">category_name = #{categoryName},</if>
            <if test="categoryCode != null and categoryCode != ''">category_code = #{categoryCode},</if>
            <if test="description != null">description = #{description},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM t_category WHERE id = #{id}
    </delete>

    <select id="selectById" resultType="Category">
        SELECT * FROM t_category WHERE id = #{id}
    </select>

    <!-- 全部分类，携带各分类下图书数量 -->
    <select id="selectAll" resultType="Category">
        SELECT c.*,
               (SELECT COUNT(*) FROM t_book b WHERE b.category_id = c.id) AS book_count
        FROM t_category c
        ORDER BY c.id
    </select>

    <select id="countBooks" resultType="long">
        SELECT COUNT(*) FROM t_book WHERE category_id = #{categoryId}
    </select>
</mapper>
