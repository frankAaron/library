<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.dao.DepositRecordMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_deposit_record (user_id, amount, type, remark, create_time)
        VALUES (#{userId}, #{amount}, #{type}, #{remark}, NOW())
    </insert>

    <select id="selectMy" resultType="DepositRecord">
        SELECT * FROM t_deposit_record WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>
</mapper>
